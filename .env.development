# Loaded for `npm run dev` / `vite` only (not `vite build`). Overrides `.env` for the same keys.
# Same hosted API as `.env.production` so local admin talks to clinicapi, not localhost:5174/api.
VITE_API_BASE_URL=https://clinicapi.mrdonline.ng/api/v1
VITE_APP_URL=http://127.0.0.1:5174
